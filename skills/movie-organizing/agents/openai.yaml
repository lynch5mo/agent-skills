interface:
  display_name: "Movie Organizing"
  short_description: "四项核心门禁：命名、孤立建夹、导演归类、质量去重"
  default_prompt: "Use $movie-organizing within the explicit TASK_ROOT. Lock scope and read the full naming contract, then run the bundled Python preprocessor plan/dry-run, safe apply, and verify before any Agent reasoning or hand-written mv; it must normalize ordinary movie folders and place reliable-CN orphan bundles inside their current director folder. Keep only NAMING_PASS/ACTION_REQUIRED/EXCEPTION, route ambiguity to _待确认_, and continue to director/collection classification and CORE_GATE then dedupe only after preprocessor verification."
