interface:
  display_name: "Movie Organizing"
  short_description: "四项核心门禁：命名、孤立建夹、导演归类、质量去重"
  default_prompt: "Use $movie-organizing only inside the user-provided TASK_ROOT. Start with scripts/movie_organizing_task.py start, then follow its fixed next_allowed command; after any compaction run status first. Do not hand-write filesystem actions or plans."
